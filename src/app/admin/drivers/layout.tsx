export default function DriversListLayout({ children }: { children: React.ReactNode }) {
  // Pass-through layout just in case we need it, mostly to ensure directory structure is valid
  return <>{children}</>;
}
